# Firmware CI — STUB (Phase 0 scaffolding, no real steps yet).
# Planned: build the ESP-IDF component/examples with `idf.py build` across a
# matrix of target boards. See docs/plan.md §3 and §10.
name: firmware-ci

on:
  push:
  pull_request:

jobs:
  build:
    name: idf.py build (stub)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: TODO
        run: |
          echo "TODO: build firmware with esp-idf across target-board matrix"
          echo "planned: espressif/esp-idf-ci-action + idf.py build"
