# Local tooling — kept out of the repo
CLAUDE.md
.claude/

# Planning doc kept local, not tracked
docs/plan.md

# macOS
.DS_Store

# Logs
*.log

# Build output (ESP-IDF, Flutter, Gradle all use build/)
build/

# ESP-IDF
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Flutter / Dart (later phases)
.dart_tool/
.packages
.pub-cache/
.flutter-plugins
.flutter-plugins-dependencies
# Libraries (not apps) don't commit their lockfile.
flutter/packages/*/pubspec.lock

# Android (Flutter app)
**/android/.gradle/
**/android/local.properties
**/android/key.properties
*.keystore
*.jks

# Editors / IDE
.idea/
.vscode/
*.iml
