# Local tooling — kept out of the repo
CLAUDE.md
.claude/

# Planning doc kept local, not tracked
docs/plan.md

# macOS
.DS_Store

# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
